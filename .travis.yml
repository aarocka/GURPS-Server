sudo: required

services:
  - docker

before_install:
    - docker pull aarocka/swift-sandbox:slim
    - docker build -t aarocka/gurps-server .

script:
    - docker run aarocka/gurps-server -c swift
